<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alpha.aoom.member.service.MemberMapper">
	
	<!-- 회원가입 -->
	<insert id="userInsert" parameterType="map">
		INSERT INTO USERS(
			USER_ID,
			USER_PW,
			USER_BIRTH,
			USER_NAME,
			USER_PHONE
		) VALUES(
			#{userId}, 
			#{userPw}, 
			TO_DATE(#{userBirth}, 'YYYY-MM-DD'),
			#{userName}, 
			#{userPhone}
		)
	</insert>
	
	<!-- 로그인 -->
	<select id="userSelect" parameterType="map" resultType="camelCaseMap" >
		SELECT
			USER_ID,
			USER_PW,
			USER_BIRTH,
			USER_NAME,
			USER_PHONE,
			USERSTAT_CODE,
			CREATE_DATE,
			UPDATE_DATE,
			USERAUTH_CODE,
			USER_IMAGE
		FROM 
			USERS
		WHERE 
			USER_ID = #{userId} AND USER_PW = #{userPw}
	</select>
	
	<!-- 아이디 중복체크 -->
	<select id="userDuplicateCheck" parameterType="map" resultType="camelCaseMap" >
		SELECT
			USER_ID
		FROM
			USERS
		WHERE
			USER_ID = #{userId}
	</select>
</mapper>