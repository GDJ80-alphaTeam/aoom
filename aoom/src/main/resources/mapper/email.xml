<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alpha.aoom.user.service.EmailMapper">
	<insert id="insertAuthNo" parameterType="map">
		INSERT INTO USER_AUTH_NO(
			USER_ID,
			AUTH_NO
		) VALUES(
			#{userId},
			#{authNo}
		)
	</insert>
	
	<select id="selectAuthNo" parameterType="camelCaseMap" resultType="int">
		SELECT
			COUNT(*) 
		FROM 
			USER_AUTH_NO
		WHERE 
			USER_ID = #{userId} AND AUTH_NO = #{authNo}
	</select>
	
	<select id="authRecord" parameterType="camelCaseMap" resultType="int">
		SELECT
			COUNT(*) 
		FROM 
			USER_AUTH_NO
		WHERE 
			USER_ID = #{userId} 
	</select>
	
	<update id="updateAuthNo" parameterType="map">
		UPDATE
			USER_AUTH_NO
		SET
			AUTH_NO = #{authNo},
			CREATE_DATE = SYSDATE	
		WHERE 
			USER_ID = #{userId} 			
	</update>
	
</mapper>
